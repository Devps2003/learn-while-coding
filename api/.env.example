# Set these in Vercel Dashboard → Settings → Environment Variables
# Comma-separated Groq keys — on 429 the API rotates to the next key
GROQ_API_KEYS=key1,key2,key3,key4
LEARNWHILE_CLIENT_KEY=learnwhile-v1
